# Local-AI-Agents-Based-on-Neural-Networks

Схема работы:
1)Микрофон $\rightarrow$ захватывает звук через sounddevice.
2)Vosk $\rightarrow$ анализирует байты звука и сопоставляет их с локальной моделью (папка model), выдает строку текста.
3)Python (MainBot) $\rightarrow$ получает строку и отправляет её через HTTP-запрос в Ollama.
4)Ollama $\rightarrow$ запускает нейросеть и выдает ответ.
5)Silero $\rightarrow$ получает текст ответа и ревращает его в звук.(Временно отказывается работать , в отпуске)

## 🛠 Зависимости

Для работы проекта необходимы следующие библиотеки:

* **requests**: Взаимодействие с API Ollama.
* **vosk**: Офлайн-распознавание речи.
* **sounddevice**: Захват аудио с микрофона и вывод звука.
* **torch**: Работа TTS-модели Silero.
* **re, json, queue**: Встроенные системные модули для обработки текста и потоков данных.

## ⚙️ Предварительная настройка

1. **Ollama**:
* Установите [Ollama](https://ollama.ai/).
* Скачайте модель: `ollama run deepseek-r1:8b`.


2. **Модель распознавания (STT)**:
* Скачайте языковую модель для **Vosk**.
* Распакуйте её в корневую папку проекта под именем `model`.


3. **Модель синтеза (TTS)**:
* Файл `model_tts.pt` (Silero) будет скачан автоматически при первом запуске.

